/**
 * 처음 부를 때 한 번만 만들고, 그 뒤로는 같은 값을 돌려준다.
 * 문제의 테스트 케이스(수만 칸짜리 스트레스 입력 포함)를 모듈을 불러오는 순간이 아니라
 * 채점·검증에 실제로 쓸 때 만들기 위해 쓴다. 문제 목록만 필요한 화면과 정적 빌드가 가벼워진다.
 */
export function lazy<T>(make: () => T): () => T {
  let value: T | undefined;
  return () => (value ??= make());
}
